name: CI

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  # Derleme sırasında backend'e istek atılmaz (tüm sayfalar force-dynamic),
  # ama ortam değişkeni tanımlı olmalı.
  NEXT_PUBLIC_API_URL: http://localhost:3001

jobs:
  kontrol:
    name: Tip denetimi ve derleme
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: pnpm

      - name: Bağımlılıklar
        run: pnpm install --frozen-lockfile

      - name: Tip denetimi
        run: pnpm typecheck

      - name: Derleme
        run: pnpm build
